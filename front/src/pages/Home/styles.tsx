import styled from "styled-components";

export const HomePageContainer = styled.div``;

export const AccordionsContainer = styled.div`
  display: grid;
  grid: 1fr / repeat(auto-fit, minmax(40%, 1fr));
  gap: 12px;
`;
